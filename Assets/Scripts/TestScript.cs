using UnityEngine;

[ExecuteAlways]
public class TestScript : MonoBehaviour {
    private void Update() {
        if (this == null) {
            
            Color c = Color.black;
        }
    }
}
